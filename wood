



local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local hrp = LocalPlayer.Character and LocalPlayer.Character:WaitForChild("HumanoidRootPart") or LocalPlayer.CharacterAdded:Wait():WaitForChild("HumanoidRootPart")

local island = "notfound"
local startfarm = true

spawn(function()
	repeat
		for _, child in ipairs(workspace.Islands:GetChildren()) do
			if child:FindFirstChild("Blocks") then
				island = child
				break
			end
		end
		wait()
	until island ~= "notfound"
end)

spawn(function()
	while true do
		startfarm = true
		for _, player in ipairs(Players:GetPlayers()) do
			if player.Name:lower() == "trimcoinn" then
				startfarm = false
				repeat
					wait()
				until not Players:FindFirstChild("trimcoinn")
				
			end
		end
		wait()
	end
end)

spawn(function()
	while true do
		if startfarm == true and island ~= "notfound" then
			local closest
			local closestDistance = math.huge

			for _, child in ipairs(island.Blocks:GetChildren()) do
				if string.find(child.Name:lower(), "tree") then
					local trunk = child:FindFirstChild("trunk")
					if trunk and trunk:IsA("MeshPart") then
						local dist = (trunk.CFrame.Position - hrp.CFrame.Position).Magnitude
						if dist < closestDistance then
							closestDistance = dist
							closest = trunk
						end
					end
				end
			end

			if closest then
				local time = closestDistance / 25
				local tween = TweenService:Create(hrp, TweenInfo.new(time, Enum.EasingStyle.Linear), {
					CFrame = closest.CFrame
				})

				tween:Play()
				tween.Completed:Wait()
			end
		end
		wait()
	end
end)

local treestarget = {
	tree1 = nil,
	tree2 = nil,
	tree3 = nil
}

spawn(function()
	while true do
		if startfarm == true and island ~= "notfound" and island:FindFirstChild("Blocks") then
			local trees = {}

			for _, child in ipairs(island.Blocks:GetChildren()) do
				if string.find(child.Name:lower(), "tree") then
					table.insert(trees, child)
				end
			end

			table.sort(trees, function(a, b)
				return (a.Position - hrp.Position).Magnitude < (b.Position - hrp.Position).Magnitude
			end)

			treestarget.tree1 = trees[1]
			treestarget.tree2 = trees[2]
			treestarget.tree3 = trees[3]
		end
		wait()
	end
end)

spawn(function()
	while true do
		if startfarm == true and treestarget.tree1 then
			local child = treestarget.tree1
local args = {
	{
		Xoeoxuqilfgenamojfjmj = "\a\240\159\164\163\240\159\164\161\a\n\a\n\a\nohIstskUiftvgjy",
		part = child,
		block = child,
		norm = vector.create(-3492.46435546875, 33.82658767700195, -3531.5),
		pos = vector.create(0, -0.00009792013588594273, 1)
	}
}
game:GetService("ReplicatedStorage"):WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("CLIENT_BLOCK_HIT_REQUEST"):InvokeServer(unpack(args))

		end
		wait()
	end
end)

spawn(function()
	while true do
		if startfarm == true and treestarget.tree2 then
			local child = treestarget.tree2

			local args = {
	{
		Xoeoxuqilfgenamojfjmj = "\a\240\159\164\163\240\159\164\161\a\n\a\n\a\nohIstskUiftvgjy",
		part = child,
		block = child,
		norm = vector.create(-3492.46435546875, 33.82658767700195, -3531.5),
		pos = vector.create(0, -0.00009792013588594273, 1)
	}
}
game:GetService("ReplicatedStorage"):WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("CLIENT_BLOCK_HIT_REQUEST"):InvokeServer(unpack(args))

		end
		wait()
	end
end)

spawn(function()
	while true do
		if startfarm == true and treestarget.tree3 then
			local child = treestarget.tree3

			local args = {
	{
		Xoeoxuqilfgenamojfjmj = "\a\240\159\164\163\240\159\164\161\a\n\a\n\a\nohIstskUiftvgjy",
		part = child,
		block = child,
		norm = vector.create(-3492.46435546875, 33.82658767700195, -3531.5),
		pos = vector.create(0, -0.00009792013588594273, 1)
	}
}
game:GetService("ReplicatedStorage"):WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("CLIENT_BLOCK_HIT_REQUEST"):InvokeServer(unpack(args))

		end
		wait()
	end
end)


